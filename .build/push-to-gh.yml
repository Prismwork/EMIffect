image: alpine/edge
secrets:
  - 26a02a13-9dec-4136-be29-490f6f150aad
environment:
  REPO: EMIffect
  GH_USER: Prismwork
tasks:
  - push-to-github: |
      cd ~/"${REPO}"
      git config --global credential.helper store
      git push --mirror "https://github.com/${GH_USER}/${REPO}"